import 'package:flutter/widgets.dart';

class RegisterPetInitialView extends StatelessWidget {
  const RegisterPetInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
